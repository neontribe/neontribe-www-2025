---
import Button from '../Button/Button.astro';

export interface Props {
  title: string;
  bodyText: string[];
  buttonLabel: string;
  buttonHref: string;
  imageSrc?: string;
  imageAlt?: string;
}

const {
  title,
  bodyText,
  buttonLabel,
  buttonHref,
  imageSrc,
  imageAlt = '',
} = Astro.props;
---

<section class="commitment-section">
  <div class="container">
    <div class="commitment-content">
      <div class="flex flex-col">
        <h2 class="commitment-heading">{title}</h2>
        
        <div class="commitment-body">
          {bodyText.map((paragraph) => (
            <p class="commitment-paragraph">{paragraph}</p>
          ))}
        </div>
        
        <div class="commitment-button-wrapper">
          <Button href={buttonHref} variant="primary">
            {buttonLabel}
          </Button>
        </div>
      </div>
      
      {imageSrc && (
        <div class="commitment-image-wrapper">
          <img 
            src={imageSrc} 
            alt={imageAlt}
            class="commitment-image"
          />
        </div>
      )}
    </div>
  </div>
</section>

